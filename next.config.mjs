/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: [
      'raw.githubusercontent.com',
      'cdn.jsdelivr.net'
    ],
  },
};

export default nextConfig;